{
  "name": "loadfix",
  "tables": [
    {
      "name": "events",
      "format": "csv",
      "partition_cols": [
        [
          "year",
          "Utf8"
        ],
        [
          "month",
          "Utf8"
        ]
      ],
      "paths": [
        "events"
      ]
    },
    {
      "name": "regions",
      "format": "csv",
      "partition_cols": [],
      "paths": [
        "regions.csv"
      ]
    },
    {
      "name": "signups",
      "format": "json",
      "partition_cols": [],
      "paths": [
        "signups.json"
      ]
    },
    {
      "name": "users",
      "format": "csv",
      "partition_cols": [],
      "paths": [
        "users.csv"
      ]
    }
  ],
  "views": [
    {
      "name": "active_users",
      "sql": "SELECT u.name, u.country, COUNT(*) AS events FROM users u JOIN events e ON e.user_id = u.user_id GROUP BY u.name, u.country ORDER BY events DESC"
    },
    {
      "name": "revenue_by_region",
      "sql": "SELECT r.region, SUM(e.amount) AS revenue, COUNT(*) AS n FROM events e JOIN users u ON u.user_id = e.user_id JOIN regions r ON r.country = u.country GROUP BY r.region ORDER BY revenue DESC"
    }
  ],
  "saved_queries": [
    {
      "name": "cross-file join",
      "sql": "SELECT u.country, e.action, COUNT(*) AS n, SUM(e.amount) AS total\nFROM events e\nJOIN users u ON e.user_id = u.user_id\nGROUP BY u.country, e.action\nORDER BY total DESC;",
      "meta": "—"
    },
    {
      "name": "top spenders",
      "sql": "SELECT u.name, SUM(e.amount) AS spend\nFROM users u\nJOIN events e ON e.user_id = u.user_id\nWHERE e.action = 'purchase'\nGROUP BY u.name\nORDER BY spend DESC;",
      "meta": "—"
    }
  ]
}
