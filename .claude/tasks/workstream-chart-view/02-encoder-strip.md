# Chart 02 · Encoder strip (x / y / series / agg)

**Workstream:** Chart (Rz2) · **Status:** ⬜ · **Depends on:** 01

## Goal
The encoding controls that map columns to chart channels.

## Current state
Not built.

## Build
- An **encoder strip**: assign columns to **x / y / series** (and the aggregate) channels, with
  type-appropriate options; changes drive the chart (task 01).

## Acceptance
- [ ] Columns can be assigned to channels; the chart updates; invalid encodings are prevented/flagged.

## Freya / references
- `docs/CHART_SPEC.md` (channels). Design: `Results.dc.html` encoder strip.
