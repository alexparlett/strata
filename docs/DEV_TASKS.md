# Strata — Dev backlog

Living backlog and source of truth for outstanding work. Design reference:
`Strata.dc.html` (**v13 handoff**) + the **`Canvas.dc.html` design system** (v13 —
canonical component/token spec; drives **S28**/**S29**) + `FEATURES.md`, `CHART_SPEC.md`,
`CONNECTIONS_SPEC.md`, and the v3 `EXPLAIN_PLAN_SPEC.md` (all in `docs/`). Product was **renamed Parquet Studio →
Strata** in the v5 redesign (section **S**). The **v6–v8** drops added the chart
view, grid selection / copy / record view, engine settings, tab drag-reorder,
launcher pinning, and a results / workspace status-bar rework — tracked in section
**R** + **S17–S24** + **B11a/b**, with per-tab result state in **A5** and view/
saved-query dirty tracking in **A6**. The **v9** drop unified Settings/Export/
Configure into floating windows (shipped via the A3/A4 `Window`); **v10** then
pulled **Connections** out of Settings into a **project-scoped sidebar pane** and
added remote (S3/GCS/HTTP) sources — tracked as **S21**. The Settings
surface is being promoted to a **separate OS window** openable from the launcher
(via the rail gear) + project windows, with settings **synced live across all open
windows** — all folded into **S22** (subsumes the old B11b). Settings is now 100%
global. **v10** also adds **column profiling** (S21-adjacent **C14**), **catalog
re-scan** (**C15**), a **permanent activity rail + Problems view** (**S23**), a
**results control-bar tighten** (**R7**), and **Configure-table honesty tidies**
(**C16**).

**Status:** ✅ Done · 🟡 Thin (wired but shallow) · ⬜ Todo · 🚧 Blocked (design pending) · ⛔ Dropped / superseded

---

## A. Architecture foundation

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| A1 | Project-scoped state | ✅ Done | `Project` domain model in `project.rs`; `AppState` holds it + engine handle + global prefs. Locked: theme/panel sizes are **global**; tables persist as **specs, re-register on open**; recents/prefs in an app-config store. |
| A2 | Action enum + dispatch | ✅ Done | One `Action` enum + exhaustive `dispatch` (`action/mod.rs`), domain handlers (`query/tab/catalog/panel/overlay`). Menus emit the same actions; only editor/cmdk bindings + modal form state stay inline. |
| A3 | Overlay architecture (containers + store) | ✅ Done | Shipped as reusable **containers** (`Popup`/`Dialog`/`Window` in `ui/components/`) + a per-window **overlay store** (`overlays.rs`, a `GlobalSignal`) driving always-mounted hosts (cmdk/Settings/Export/Config); co-located popups (menus/project/remove/cell) stay on local `use_signal`. Killed the app-global `*_open` bools + `CloseOverlays` coupling. Not the `Vec<Modal>` stack originally sketched — stacking deferred (the `EscStack` upgrade path). See `OVERLAY_ARCHITECTURE.md`. |
| A4 | Modal form state off `AppState` | ✅ Done | **Export:** component-local `use_signal`; `RunExport(opts)` carries the snapshot. **Config:** component-local `draft` seeded from a store `ConfigTarget` (`New`/`Edit(name)`); the project stays **immutable until a successful register** (no placeholder). `RegisterTable(draft)` sends the engine spec + stashes the row in `overlays::pending_register`; on `Registered` the success path builds the real catalog row from the stash + engine columns and autosaves (engine events skip the dispatch autosave), the load-time path updates the existing row, and failure shows an inline `config_err` with the window still open. `AppState` now holds neither `cfg` nor `export`. See `OVERLAY_ARCHITECTURE.md`. |
| A5 | Per-tab query state | ✅ Done | **Results / plan / error scoped to the tab** (FEATURES §10). Query output moved off `AppState` into **`crate::runs`** — a per-window `GlobalSignal<HashMap<u64, WorkspaceRun>>` keyed by tab id (chosen over `AppState.runs` for reactivity isolation: AppState is one coarse `Signal`, so the store keeps find-in-results / paging / plan-toggle re-renders on the results panel only). Reducer routes by `ws_id` via `runs::is_pending` + `edit_existing` (drops results for closed/superseded tabs); reaped on tab close, cleared on project open. Also decoupled persistence: `project::Workspace` is now `{ id, name, sql }` with **no `serde(skip)`** — the tab `id` persists (`normalize` repairs legacy/dup). App-bar `status_text` stays global; results-panel status (R1) derives from the active tab's run. Compiles + runs. |
| A7 | Split `ui/workspace.rs` into a module | ✅ Done | Broke the 683-line `workspace.rs` into `ui/workspace/` — `mod.rs` (`Workspace` shell), `tabs.rs` (`Tabs`), `editor.rs` (`Editor`), `results.rs` (`Results` switch + `ResultsToolbar` / `Pager` / `Running` / `ErrorView` / `Empty` / `EmptyState` → future **R1**), `grid.rs` (`ResultsGrid` + `CellDialog` → future **R3–R6**), `plan_view.rs` (`PlanView` → future **S20**). Each pane is a `#[component]` pulling `AppState` from context → **independent reactive scopes** (typing in the editor no longer re-renders the grid); the tab menu + cell view are now component-local signals, not threaded from `Workspace` / `AppState`. Leaf render helpers called per-item (`render_cell`, `plan_node_card`, `fmt_int`, `tab_menu_items`) stay plain fns. `Workspace` is the only `pub` item. |
| A6 | Tab architecture: `.strata/` split + dirty tracking | ✅ Done | **Persistence split** (A6.1, matches Athena/VS Code): project is now a `.strata/` dir — `project.json` (committed: tables/views/saved-queries) + `session.json` (gitignored: tabs/active/history/geometry) + auto `.gitignore`. Runtime `Project` stays unified; split at load/save via `DefsFile`/`SessionFile` DTOs. Migrates legacy single-file `*.strata`/`*.psproj` on open (legacy left in place). Autosave routes: def-touching actions write both, session-only actions write `session.json` only (`touches_defs` in `dispatch`; reducer autosaves defs on view-change/deregister). **Binding + dirty** (A6.2–4): `Origin { Scratch, View(n), SavedQuery(n) }` + `origin_hash` (FNV-1a of the bound SQL) on `Workspace`, set on open (edit-view/open-saved/select-*) and rebound on ⌘S/save-as-view; `is_dirty` = a View/SavedQuery-bound tab whose SQL diverged from its baseline hash; **scratch tabs (Tier 2 session buffers) are never dirty** (they have no committed def to diverge from, and restore from `session.json`). Tab dirty-dot (orange `.tdot`) + header Save emphasis; discard-on-close confirm via `overlays::close_confirm` + `CloseConfirmHost` + `CloseTabForce`. Supersedes C1. **Unverified — needs compile.** |
| A8 | Intercept OS-triggered closes (themed) | ⬜ Todo | Extend S14's running-query confirm to the **OS close paths**, keeping our themed `RunningClose` dialog. **(1) Per-window OS close** (red button / menu Close): tao can't veto from a wry handler → dynamically set `WindowCloseBehaviour::WindowHides` **while a query runs** (+ setting on), else `WindowCloses`; on `CloseRequested` re-show (`set_visible(true)`) + open the dialog; "Stop & close" / `close_now` flip back to `WindowCloses` + `ctx.close()`. Moderate, **no unsafe**. **(2) App quit** (⌘Q / Apple-menu / **dock** Quit): only `NSApplicationDelegate applicationShouldTerminate:` covers all three (a custom muda ⌘Q misses the dock). tao leaves the selector unimplemented → add via `objc` `class_addMethod` on `NSApp.delegate.class`. A themed dialog needs `NSTerminateLater` + a bridge from the raw callback into dioxus (broadcast → per-window coroutine, focus-gated) + `replyToApplicationShouldTerminate: YES/NO` on the button click. **Unsafe objc** (type-encoding `"L@:@"`, fn transmute, main-thread install after the loop is up) + an **unknown** (does the webview pump during AppKit's terminate-later run loop?) → **iterate-together, not a blind one-shot**. Refs: Apple *Graceful App Termination*; tao `app_delegate.rs` (no `applicationShouldTerminate`); tauri #12978 / #9198. |
| A9 | DataFusion 43 → 54 upgrade | ✅ Done | **Compiled + running on 54** (bump + F5 function-push + `engine.rs` API migration; the sqlparser-0.62 `ddl.rs` fixes). Unblocked S26/S7/S25 (shipped) + S21.** Bumped `datafusion` 43 → **54** (arrow 53 → 58, object_store 0.11 → 0.13, sqlparser 0.51 → 0.62, + API churn across 11 majors) and fix `engine.rs` (register/query/explain/export) + anywhere Arrow/object_store types surface — plan + risk order in **`docs/A9_DATAFUSION_UPGRADE.md`** (Listing tables → physical-plan/Explain → rest). **Why 54 gates the language service:** only 54's `FunctionRegistry` enumerates *all* function categories by name (`udfs`/`udafs`/`udwfs`/`higher_order_function_names`) — 43 exposes scalar names only; 54 also ships planner `Diagnostic`s with **source spans** (semantic validator) + the newer `sqlparser`/`DFParser` we reuse. Prerequisite for S26/S7/S25; unblocks S21. See `SQL_LANGUAGE_SPEC.md` (F0). |

## B. v2 design (all shipped)

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| B1 | Event Log panel | ✅ Done | Fed from real engine events (query/register/view/export/errors), colour dot by kind, cap 200. Now folded into the S5 drawer. |
| B2 | Catalog row context menu | ✅ Done | Kind-specific items (table/view/saved-query), cursor-positioned, backdrop closes. |
| B3 | Workspace tab context menu | ✅ Done | Rename / close / others / right / all / reopen (⇧⌘T) + closed-tab stack. |
| B4 | Inline rename | ✅ Done | Tabs (inline input, Enter/Esc/blur) + catalog objects (engine rename = deregister+register / drop+create). |
| B5 | Remove-confirmation dialog | ✅ Done | "Drop table/view?" modal gating the sidebar ✕ and context-menu Remove. |
| B6 | Resizable panels | ✅ Done | Drag handles for sidebar / inspector / editor / log sizes; clamped; body cursor on drag; persisted. |
| B7 | Saved Queries catalog section | ✅ Done | Project SQL snippets (distinct from real views) + "Save query to project" editor action. |

## B2. v4 design

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| B8 | Welcome / Launcher + multi-window | ✅ Done | `window.rs`: each project its own window + engine; separate launcher window (opens only when the last project closes); ⌘` cycling; titlebar drag; per-project geometry in `.strata`; per-engine snapshot scoping. **Thin:** geometry in physical px (off on mixed-DPI). |
| B9 | Collapsed sidebar rail | ✅ Done | 46px icon rail (expand / catalog / new-table) when `!sidebar_open`. |
| B10 | Open in current-vs-new window prompt | ✅ Done | When `open_pref == Ask`, opening from a project window (Open Project **or** Open Recent) shows the prompt — This Window / New Window / Cancel + a "remember, don't ask again" toggle that persists the choice. `overlays::open_prompt` + always-mounted `OpenPromptHost` → `OpenPromptCard` (child mounted only while open, so the checkbox resets each open) + `Action::OpenChosen`; `projects::open_with_pref` routes this/new/ask. Launcher unaffected (spawns windows directly). Also folded in: `open_pref` `String` → `config::OpenPref` enum (serde lowercase, back-compat), and a reusable `ui::components::Checkbox` (button `role=checkbox`, controlled, our own — dioxus-primitives is unreleased + needs the CLI). |
| B11a | Launcher project actions + pinning | ✅ Done | Launcher Projects pane (§0): per-row actions — **Pin/Unpin** (PINNED/RECENT sections), **Open in new window**, **Reveal on disk**, **Remove from list** — plus a **search** box over name/path, colour-initial avatars, and a pin badge. Row actions `stop_propagation` over the row's open-click; reactive `recents` signal re-renders on mutate. Shipped alongside: **reopen the whole set of open projects on startup** — `AppConfig.open_projects` (added on `install`, removed on every window-close path), `decide_startup` reopens all still-existing entries; setting renamed "Reopen projects on startup". |
| B11b | Launcher Settings entry point | ⛔ Folded into S22 | v10 de-duplicated the launcher settings (no inline pane; a rail gear opens the shared Settings window). Since that Settings window is being promoted to a **separate `wry` window openable from launcher + project** with **cross-window sync**, the launcher-gear entry point is now part of that work — tracked under **S22**. |
| B12 | Tab: Duplicate | ✅ Done | **v10:** a **Duplicate** entry in the per-tab right-click menu (between Rename and Close) — clones the tab's SQL into a new `Origin::Scratch` "&lt;name&gt; copy" tab inserted immediately to its right + focused. `Action::DuplicateTab(id)` → `tab::duplicate` → `session::duplicate` (insert at pos+1 via the `workspaces` lens; persisted id; unique-name guard); classified session-durable (autosaves `session.json`). **Unverified — needs compile.** |

## S. Strata redesign (v5 handoff — current priority)

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| S1 | Rename to Strata + new icon | ✅ Done | Renamed Parquet Studio → Strata; sediment-layers logo; `.psproj` → `.strata` (opens legacy too); app icon via `Dioxus.toml [bundle]` + runtime `with_window_icon`; full branding sweep. |
| S2 | Theming: JSON theme files + token injection | ✅ Done | Themes are JSON (`theme.rs`) — token map + `extends` + mode; loader discovers built-ins + user dir; injected as `--*` CSS vars; whole stylesheet swept to `var()`; persists to config. **Remaining:** plugin theme dirs, live reload. |
| S3 | Settings modal | ✅ Done | `⌘,` modal, 4 categories (Appearance / Data display / System / Keymap); all prefs **persist + enforced**. Follow-ups owned elsewhere: Ask-prompt → B10, confirm-close → S14, OS-sync live listener + search box + reload-themes (minor). |
| S4 | Toolbar moved into header | ✅ Done | Run (accent + ⌘↵) / Format / Clear / Save-view / Save-query now header icon-btns (shown when a tab is open); editor run-bar removed; dropped DataFusion badge + proj meta. Run↔Cancel toggle is S14. |
| S5 | History + Events → tabbed bottom drawer | ✅ Done | One drawer (`drawer.rs`) with History / Events tabs, tab-aware Clear, expand/close; history single-click loads, double-click runs (idempotent). Added `LogKind::Run`/`Warn`. |
| S6 | Error view on failed queries | ✅ Done | Results-area error banner (typed class + `line:col` + code frame + caret + hint) **and** expandable Events rows, via shared `errview::error_detail`; `query_error.rs` parses DF error strings (unit-tested). Follow-up: engine-surfaced line/col. |
| S7 | SQL autocomplete | ✅ Done (`9bad10a`) | Caret-anchored dropdown driven by `crate::sql::complete` + the vendored editor's caret channel; ↑↓/Enter/Tab/Esc + click accept. Context-aware completion (tables after FROM/JOIN, columns after `alias.`, else pooled), caret dropdown, ↑↓/Enter/Esc. Follow-ups: ⌘Space trigger, flip-up, popup anchor tuning, caret-after-accept (needs the `eval` refinement). **Design jointly with S25** on a shared SQL-language-service foundation — **full combined design `docs/SQL_LANGUAGE_SPEC.md`** (feasibility in `docs/EDITOR_LANG_SPIKE.md`). Depends on **S26** (foundation). Autocomplete work here = the completion provider (context rules + ranking) + the dropdown UI (caret-anchored, ↑↓/Enter/Esc/Tab, flip-up, accept-replaces-token). (Corrects the old note: the editor **is controlled** and is a real `<textarea>`; caret/coords via a `document::eval` bridge, resolved by vendoring the editor in S26.) |
| S8 | Tab-bar controls | ✅ Done | **Pinned right cluster** (`.ws-tab-cluster`, border-left off the scrolling strip): **+ new-tab**, **▾ show-all-tabs** (searchable `Popup` — filter box + rows **most-recently-viewed first, capped at 10** with a "+N more — keep typing to filter" hint; active/dirty dot, click switches / × closes / Enter opens first match). MRU via a `Workspace.last_viewed` stamp + `Session.view_clock`, bumped through a single `session::set_active`, **⋯ overflow** (`Popup` — Close all / Close other / Reopen ⇧⌘T). New-tab moved out of the scroll into the cluster. In `ui/workbench/tabs.rs` (`overflow_menu` / `tab_list` / `tab_list_query` signals + `overflow_menu_items` / `tab_list_body`). **Unverified — needs compile.** |
| S9 | Catalog row menu polish | ✅ Done | Vertical-⋮ row menu (Open / Configure / Edit / Rename / Remove); fixed the ellipsis glyph (was horizontal). |
| S10 | Reduce redundant status text | ✅ Done | The redundant right-aligned `rows · ms · cols` readout is gone from the status bar (empty right/spacer, matching the design); result stats live only in the pager. The left status line legitimately shows the latest log/status message. |
| S11 | File menu (native app menu) | ✅ Done | Native menu bar (`src/menu.rs`, attached via `Config::with_menu` on both window configs): rebuilt **App / Edit / Window** (Edit from `PredefinedMenuItem`s so copy/paste/undo survive replacing dioxus's default) + a **File** menu — New Query · Open Project (⌘O) · Open Recent (config recents, capped 10) · Close Project · Save All (⌥⌘S) · Settings. Drops the debug dev-tools Help menu. Ids are a `MenuCmd` enum (`Into<MenuId>` to build, `parse` to recover — one source of truth). The macOS menu is app-global, so each `ProjectRoot` tracks focus (`WindowEvent::Focused` → `window::note_focused` thread-local) and its `use_muda_event_handler` acts only when focused, relaying the id into a signal a `use_effect` consumes (so the async open dialog has a scope). No accelerators on webview-bound shortcuts (⌘T/⌘W/⌘S/⌘,) to avoid hijacking `handle_key`. *Not in design — Alex's add.* **Unverified — needs compile.** Follow-ups: launcher-focused File clicks are inert (project windows only); Open Recent is a build-time snapshot; Settings → the S22 global window when it lands. |
| S12 | EXPLAIN query-plan view | 🟡 Thin | Dedicated engine path walks DataFusion's **typed** LogicalPlan/ExecutionPlan + live `MetricsSet` — **no text/JSON parsing**. Physical/Logical tabs (both modes), Raw/Tree, HOTSPOT + per-node metrics. Reflects the pre-v3 design; **v3 rework outstanding → S20** (self-time, 3-tier metrics, detail field-rows). ANALYZE still buffers via `collect` (stream-drain follow-up). |
| S13 | Query-lifecycle states in results area | ✅ Done | Four-way switch: no-results / running spinner / error banner / grid. Cancel button on the Running state → S14. |
| S14 | Query cancellation + confirm-on-close | 🟡 Thin | **Cancellation done (unverified — needs compile):** engine runs Query/Explain as **abortable `tokio` tasks** in an inflight map keyed by `ws_id` (loop stays free to receive `Command::Cancel{ws_id,req_id}` → abort → drop DataFusion stream cancels cooperatively → `Event::QueryCancelled{…,elapsed_ms}`); re-run supersedes+aborts; `CleanupWorkspace`/`All` abort first. Reducer clears running (keeps prior result) + `warn` log + a `cancelled` `HistoryItem` (`#[serde(default)]`). UI: `Action::CancelQuery`→`query::cancel` (scoped to `pending_req`); editor **Run↔Cancel** (red) while running; results Running-state Cancel; **Esc** = overlay open ? `CloseOverlays` : `CancelQuery` (`overlays::any_open`). Side benefits: superseded queries stop wasting CPU + concurrent per-tab queries. **Confirm-close-while-running (done, unverified; v11 design):** one shared **mode-driven** `RunningCloseHost`/`Card` dialog, two entry points, gated by `confirm_close_running` — **Window** (Close Project) confirms if any tab is running → "Confirm exit" / "Stop & exit" (log-out icon) → `CloseWindowForce`→`projects::close_now`; **Tab** close confirms whenever the tab has a query **in flight** (**no threshold** — a finished query has `running==false`, so quick queries never nag) → "Confirm close" / "Stop & close" (stop icon) → `CloseTabForce`. Amber warning header icon + name subtitle + divided footer + our `Checkbox` (v11: 16px, box-sizing). Dirty tabs still hit the A6 discard dialog first. "Don't ask again" flips the setting (Host/Card split resets the checkbox); setting label "…a **tab or window** with a running query". *Caveat:* the window confirm is on **Close Project** only — OS red-button / ⌘Q are the deferred **A8** task. |
| S15 | System / behaviour settings | ✅ Done | System Settings category — reopen-last (threaded into startup: launcher vs project window), default project dir, opening-a-project pref, row limit — all **live + persisted**. Settings moved off `AppState` into a per-window `settings` `GlobalSignal` store (flattened into `AppConfig`); density/zebra now reactive, OS-sync follows `ThemeChanged` live. Remaining slivers owned elsewhere: the confirm-on-close **dialog** → S14; settings search box → S18; Engine category → S17. See `settings.rs`. |
| S16 | Author-friendly theme files + JSON Schema | ⬜ Todo | Restructure theme JSON into named groups + ship `theme.schema.json` (autocomplete/validation); loader flattens groups → `--*`; keep flat-map back-compat; regenerate `REQUIRED_TOKENS` from the schema. |
| S17 | Engine settings category + SQL escape hatch | ⬜ Todo | 5th Settings category (§15): a curated **~11-knob** subset of DataFusion `ConfigOptions` grouped Execution / Memory & spill / SQL parser / Result format / Optimizer — type-aware controls (number / text-with-suffix / segmented / toggle), each row shows the full `datafusion.*` key, a changed row gets a **MODIFIED** badge + per-row reset, and a **Reset all (n)** clears every override. Only *overrides* are stored (`engineCfg`), so `SHOW`/reset stay honest; `format.null` wired **live** to the grid. **Editor escape hatch:** a lone `SET` / `RESET` / `SHOW` / `SHOW ALL datafusion.*` is intercepted in the run path (no scan) → applied to overrides (`SET`/`RESET` confirm via event log + toast; `SHOW`/`SHOW ALL` open a synthetic config result set); unknown `datafusion.*` keys accepted; non-`datafusion.` statements fall through as queries. Every knob searchable in the settings box. |
| S18 | Settings: history-limit + search box | ⬜ Todo | **Query-history limit** control (25 / 50 / 100 / 200, default 50) under System → History; lowering it trims the current list immediately and new runs drop the oldest once capped. **Settings search** box above the category nav — a live index over label / keywords / category that replaces the category list while typing; Enter jumps to the top hit, clicking a result switches to its category + scrolls it in + briefly flashes the setting, Esc clears. (S3 flagged search as a minor follow-up.) |
| S19 | Tab drag-to-reorder | ⬜ Todo | Press-drag a tab: a solid clone lifts + follows the cursor, the source dims, an accent insertion line shows the drop slot; drop commits the reorder (active tab tracked by **identity** so focus never changes). Auto-scroll near either overflow edge; a 5px threshold keeps plain clicks + the ✕ working; suppressed while a tab is being renamed. Pointer events + a floating clone (native HTML5 DnD doesn't fire reliably in the webview). Distinct from S8's tab-bar controls. |
| S20 | EXPLAIN plan → v3 rework | ⬜ Todo | Rebuild the plan view to `EXPLAIN_PLAN_SPEC.md` **v3** (supersedes S12's shape). Engine sends a derived **self-time** per node (§7: scan `processing`, join `build+join`, exchange `repartition`, else `elapsed_compute`) driving the headline time chip, the time-share bar, and **HOTSPOT = self-time ≥ 60% of max**. **3-tier metrics** (ANALYZE, physical tab): tier-1 headline (`rows` · self-time · `bytes` · bar) · tier-2 priority-ordered non-zero insights (errors → spills → row-group pruning → pushdown → peak/build mem) · tier-3 collapsed **typed grid** grouped Output / Time / I/O / Pruning / Memory / Exchange / Join / Errors / Other, category-coloured left bars, values coloured by metric `type`, per-node **show-zeros** toggle. `detail` parsed into aligned **key/val field rows** (top-level-comma split, bracket-aware; clamp-by-field-count + show-more). Toolbar de-dup (drop the redundant `Logical plan · N` text). |
| S21 | Connections pane + remote object-store sources (v11) | ⬜ Todo | **Full spec: `docs/CONNECTIONS_SPEC.md`.** Read parquet/csv/etc. from remote object stores (**S3, GCS, HTTP** — **Azure dropped in v11**; S3-compatible R2/MinIO/OSS/COS ride S3 + a custom endpoint) via **project-scoped connections** — a sidebar tool-window on the **Catalog \| Connections** activity rail (not a Settings category). **No app-managed secrets:** stores only non-secret per-provider metadata; **provider is an explicit picker** (S3/GCS/HTTP), auth is per-provider (S3: Ambient / Named profile / Anonymous + Region-required + optional Endpoint/Allow-HTTP; GCS: Ambient/ADC / SA-file-path / Anonymous; HTTP: anonymous). For AWS we **wrap the `aws-config` provider chain in an `object_store::CredentialProvider`** (register per bucket). Configure-table gains a **LOCATION** segmented (Local disk / Object store); object-store tables are **single-path** (text-only, no Browse) against a chosen connection (custom dropdown). Connections persist (non-secret) in `.strata/`. *(v9 window unification shipped via A3/A4; Settings 100% global. Row menu edits only; provider badges.)* |
| S22 | Settings as a separate `wry` window + cross-window sync | ⬜ Todo | Promote the Settings surface from a per-VirtualDom overlay to its **own OS-level `wry` window** (like the launcher / project windows in `window.rs`), **openable from both the launcher and any project window** — one canonical Settings window, focused/raised if already open rather than duplicated. **Launcher entry (subsumes B11b):** the launcher rail is **Projects** (top) + a **Settings** gear (bottom, IntelliJ-style) that opens this window; the workspace opens the same one (`⌘,` / header gear). **Cross-window settings sync:** settings today live in a **per-window `crate::settings` `GlobalStore`** (one per VirtualDom), so a change in the separate Settings window must propagate **live to every open project window** (+ launcher). Needs a broadcast path — e.g. on a settings mutation persist to `AppConfig` then notify all registered windows (the `window::WINDOWS` weak-context registry) to reload their store lens, or a shared channel — so theme / density / zebra / row-limit / open-pref / etc. update everywhere without a reload. Persist stays in `AppConfig`; the Settings window closes independently of its opener. |
| S23 | Permanent activity rail + drawer reparent + Problems view | ✅ Done | **Unverified — needs compile. Deferred: Connections rail button → S21; Problem-row token-select (jumps to the owning tab for now). v10 (RustRover model) — supersedes B9, extends S5 / B1.** Replace the disappearing collapsed-sidebar stub with a permanent **48px activity rail**: top group = tool windows (**Catalog** toggle — active = accent icon + left stripe + tinted bg); bottom group = **Problems / Events / History**, each toggling the bottom drawer to that view. **Problems** is a real diagnostics view (not chrome): filtered `error`-kind events, a **red count badge** on the rail icon (`99+` cap), **grouped by owning query tab** (sticky headers), flat one-liner rows (icon + message + `:line`); clicking a row **switches to that tab + selects the offending token**; empty "No problems" state. **Drawer reparent:** the drawer moves into the right-area column (after the rail) so opening it pushes catalog + editor + results + inspector up but leaves the rail full-height; its in-drawer tab strip is dropped (rail drives the view) → header is just `title · count` + Clear / expand / close; **drag-resizable** from the top edge (140–680px). **Events flattened** to a pure log (Problems owns rich diagnostics). **Bottom 26px status footer removed** — role covered by the rail + results status bar. |
| S25 | Problems as first-class diagnostics + static SQL validator | ✅ Done (`e92ed90`+`ac68810`) | **Supersedes S23's log-derived Problems.** Problems is now a live **per-tab diagnostics set**, not a filtered event log — fully decoupled from `crate::state.log`. New `crate::diagnostics` per-window store (`DIAGS: GlobalStore<HashMap<ws, Vec<Diagnostic>>>`) holds **validation** diagnostics (static analysis, no run); **execution** errors are *unioned in* from `runs.query_error` (already lifecycled — set on failure, cleared on rerun-start + success), so a fixed-then-rerun query clears itself for free. `revalidate(ws, sql)` runs **debounced on every SQL edit** via a **per-tab** `use_revalidate` hook in the `Workspace` view (subscribes to only that tab's `sql` lens — own debounce per tab, and background tabs edited programmatically revalidate too) so a typo like `FORM` clears the instant it's fixed — **no execution**. Problems view + rail badge + drawer count all read `diagnostics::{problems_for, total_errors}` reactively; the old `AppState::clear_problems` hack + `LogEvent.ws` grouping are gone. **Validator** (`crate::sql::validate::analyze`): unbalanced parens + keyword-typo `FORM→FROM` lints (edit-distance, conservative) → inline squiggles (via the vendored editor's `decorations`) + Problems. Semantic checks (unknown table/column) deferred to a future engine dry-plan. Built jointly with S7 (autocomplete) on the shared SQL-language-service foundation — the tokeniser, caret-context, and catalog symbol model are the same for both. **Full combined design: `docs/SQL_LANGUAGE_SPEC.md`** (feasibility in `docs/EDITOR_LANG_SPIKE.md`). Depends on **S26** (foundation). Validator work here = the structural + semantic rule tiers + squiggle rendering; also add `Diagnostic.span` (byte range) for squiggles + click-to-select. |
| S26 | SQL language service foundation (vendored editor + analysis layer) | ✅ Done (`e92ed90`+`ac68810`) | Shared foundation for S7 + S25 — full design `docs/SQL_LANGUAGE_SPEC.md`.** Three pieces: (1) **vendor `dioxus-code-editor` 0.1.2** into `src/ui/code_editor/` (copy lib.rs + edit_capture.rs + desktop.rs + CSS; **strip** the wasm `web.rs` + web-sys/wasm-bindgen; props stay a superset so `editor.rs` is unchanged), then **add** an `oncaret`/`onkeydown` surface (the existing no-op `onmounted` seam gives the textarea `MountedData`; selection read is a ~5-line `document::eval` unless dioxus 0.7 `MountedData` already exposes it) and a `decorations` overlay layer (squiggles/gutter, monospace-metric positioned). (2) **`crate::sql`** — **reuse DataFusion's `sqlparser` tokenizer + `DFParser`** (via the `datafusion::sql` re-export, so dialect/version match the engine — *not* a hand lexer) for `Token{kind, span}` + a full-parse validity check → statement split → clause-context state machine (`AfterFrom/AfterJoin/AfterDot(alias)/SelectList/WhereExpr/…`) → symbol resolver; `analyze(sql,&cat)` (validation) + `complete(sql,caret,&cat)` (completion). (3) **Symbols:** tables/columns are already there — `project.tables`/`.views` carry `columns: Vec<ColumnInfo>` (no schema-push needed); **functions** need a small engine push — enumerate the full registry (`udfs`/`udafs`/`udwfs`/`higher_order_function_names`) → `Event::Functions` → UI `FunctionCatalog`, so completion offers real built-in + UDF functions and the validator can flag unknown calls. **Depends on A9 (DataFusion 54 upgrade)** for the registry enumeration + newer sqlparser + planner-diagnostic spans. Build order F0–F5 in the spec. |
| S24 | Keymap rebinding (Settings → Keymap) | ⬜ Todo | **v11 FEATURES §15/§16** — the prototype has full rebinding; our Rust `handle_key` matches hardcoded defaults. Make each Keymap row **click-to-capture a chord**, with inline **conflict detection** against other bindings, **per-row override + Reset all**, overrides persisted as **`keymapOverrides`** (machine-global). Rewire `handle_key` to resolve the pressed chord through the **override-merged keymap** (default bindings + overrides → effective action) instead of literal ⌘K/⌘T/⌘W/⌘S/⌘,/⌘↵/⌘\` matches — needs a keymap data model (action ↔ chord). Note the interaction with **S11** native-menu accelerators (deliberately kept off webview-bound shortcuts). |
| S27 | Editor squiggle **hover popover** (diagnostic message) | ✅ Done (mechanism) · ⚠ restyle → S29 | **Mechanism compiled + verified on Mac.** `editor.rs` `mousemove` hit-tests the diagnostics' span boxes (fixed monospace metrics) → a client-px anchor below the token, rendered via the reusable `Popup` in **hover mode** (`backdrop:false` — pointer-transparent, no focus-grab; a single render tree, see S29). Suppressed while the completion menu is open; cleared on `mouseleave`/move-away. **⚠ v13 styling gap:** the card should be the **§08 error-callout** (neutral dark bg, **3px** red left bar, red circle-info icon, **light** message `--text2`) — mine is a redder-tinted card with a 2px bar + warning-triangle icon. **Folded into S29:** the lint popover adopts the S29 tooltip/callout card there rather than hand-styling `.sql-lint-pop` now. **v12.** Hovering a red squiggle shows a small floating popup with the diagnostic **message** (icon + text, red left-border, styled like the results error banner) — **hover-only, no header**; the persistent squiggle stays the always-on signal, the message is on-demand to keep the editor minimal. **Suppressed while the autocomplete dropdown is open** (avoid overlap). The data is already there: `crate::diagnostics` carries `message` + byte `span`, and the vendored editor already renders squiggle `decorations` — so this is a **UI-only add in `editor.rs`**: hit-test the pointer against the decoration span rects on `mousemove`, anchor a popup below the hit token, clear on `mouseleave`/move-away. Natural extension of **S25** (design calls it out as the squiggle "error popup"). |
| S28 | v12 input & control component refactor (app-wide sweep) | ⬜ Todo | **v12's "sweeping input interaction & design refactor."** **Canonical spec: the `Canvas.dc.html` design system (v13), distilled in `docs/DESIGN_SYSTEM.md` (token map + per-component tables)** — §03 Buttons (primary / secondary / ghost / danger / soft + icon **toolbar** / **ghost** / **pager** / **toggle** variants; 34px, radius 8px), §04 Inputs (34px, radius 8px; focus = `inset 0 0 0 1px accent, 0 0 0 3px color-mix(accent 20%)`), §06 Selection controls (**segmented** pill selected = `color-mix(accent 12%)` tint + accent text — **not** solid; **toggle** 34×19 track + 15px knob; **checkbox/radio** 16px, accent when on). Design tokens are `--c-*` (e.g. `--c-panel`, `--c-border2`, `--c-onaccent`) → **map to our `--*`** (`--accent`/`--panel`/`--accent-soft`/…). *(v13 confirms the segmented + icon-toggle tint work already shipped in R1.)* Build a set of **reusable, app-themed control components** in `src/ui/components/` and sweep the app to replace the ad-hoc inline class-string controls with them — so the v12 look/behaviour is consistent and controls stop being re-hand-rolled per modal. Scattered inline today: `.btn`/`.btn-ghost`/`.btn-danger`, `.icon-btn`(+`.plain`/`.dirty`)/`.mini-btn`/`.pg-btn`/`.proj-btn`/`.rail-btn`/`.cmdk-btn`, `.seg`/`.seg-btn`/`.seg-row`, `.toggle`+`.knob` (repeated ~4× in settings alone), `.input`/`.field`/`.src-input`/`.page-input`/`.cmdk-input` (the anchored `.menu` **dropdowns** are **S29**). **Components** (Checkbox already exists — `checkbox.rs`): **Button** (variants: primary / ghost / danger / **borderless-icon** — the `.icon-btn.plain` we already use for the Inspector & drawer close X / mini); **TextInput / Field** (icon-slot field wrapper + input, incl. escape-aware inputs); **SegmentedControl** (single-select "multi-button" — `.seg`/`.seg-btn` + active state; used by density / row-limit / open-pref / export format / future LOCATION·TYPE·PROVIDER pickers); **Toggle/Switch** (the `.toggle` pill + `.knob`); and any **multi-switch / multi-position** control the design introduces. (Dropdowns/menus are the **S29** overlay family — the `Select` there is a consumer of `Popup`, not a form-control here.) Then **adopt across** settings / export / config / sidebar / launcher / command palette. **Foundational** — the other v12 items consume these + **S29**'s `Select`: the escape-aware **TextInput → CSV delimiter** and **S29 `Select` → CSV compression** both land in **C17** (export owns its own surface), and the **chart encoders / S21 connection AWS-profile picker / engine-settings enum rows** (none built yet) adopt the S28 + S29 components when their features land. Land S28 first or alongside C17. *(No separate "borderless sidebar header" item — borderless buttons already exist via `.icon-btn.plain`; the design's sidebar refresh / connections-add borderless buttons ride C15 / S21 and just use the Button borderless-icon variant.)* |
| S29 | Overlay/menu component family (Popup base + ContextMenu + DropdownMenu/Select) | ⬜ Todo | **v12 component sweep (paired with S28).** **Canonical spec: `Canvas.dc.html` (v13), distilled in `docs/DESIGN_SYSTEM.md`** — §05 Dropdowns (trigger 34px + chevron; popup `--c-elev` bg / `--c-border3` / radius 9px / `box-shadow 0 12px 30px`, selected row = `color-mix(accent 10%)` + accent text) and §07 Menus / Context menu / Tooltip (shared card chrome: elev bg, border3, radius 9px, 4px padding, 1px row gap; tooltip 220px, arrow). Factor the anchored-overlay family the way `dioxus-primitives` does — the split is about the **trigger + interaction contract**, not the positioned card (that's shared). **`Popup`** (exists) stays the dumb base: portals a positioned card with an optional dismiss **`backdrop`** (already generalised for the S27 lint tooltip — `backdrop:false` = pointer-transparent, no focus-grab, one render tree). Build the trigger+contract wrappers **on top** of it, each owning its own trigger, dismissal, keyboard/a11y, and **trigger-relative anchoring** (kept in the wrapper so `Popup` stays lean, not a flag-pile): **`ContextMenu`** (opens on `oncontextmenu` at the cursor; menu semantics — arrow-roving / Enter / Esc — replacing the ad-hoc catalog-row + tab right-click menus), and **`DropdownMenu`/`Select`** (opens on click, anchors *to the trigger* — below, width-matched, flip near edges — value + chevron button, single-select + checked row; generalises `config.rs`'s inline Format picker → the design's `selMenu`/`selPick`/`_selView`). This `Select` is what **S28** form surfaces + **C17** compression + future chart/S21/engine pickers consume. **Tooltip / callout card:** define the shared tooltip/callout card styling (canvas §07 tooltip + §08 error-callout — elev bg, `--line3` border, r9, shadow; error variant = 3px red left bar, circle-info icon, light message). **The S27 lint popover adopts it here** (finishing S27's v13 restyle — it stays a *controlled* `Popup{backdrop:false}` with its bespoke squiggle hit-test, just re-skinned onto the callout card). No hover-trigger `Tooltip` *wrapper* yet (icon buttons use native `title`) — add one only when a simple element tooltip appears. Keep the **autocomplete menu bespoke** (caret-anchored + keyboard nav + live filter), not a `DropdownMenu`. |
| S30 | Editor Run **split button** (Run / Explain plan / Explain analyze) | ⬜ Todo | **v13.** The editor-toolbar **Run** becomes a **split button**: the main face runs the query as-is; an attached **caret** opens a small menu — **Run** · **Explain plan** · **Explain analyze**. Picking a mode **rewrites the editor buffer** with the correct prefix (`EXPLAIN …` / `EXPLAIN ANALYZE …`, **stripping** any existing prefix) and runs immediately; a **check mark** marks the current mode. The **caret is hidden while a query is running** — the control collapses to the plain **Cancel** button, so the split affordance only shows when there's something to launch (same underlying run/cancel path, mode applied). **Manually typed `EXPLAIN` / `EXPLAIN ANALYZE` must still work** — the run path already detects them (`crate::plan::is_explain` + the analyze variant), so a hand-typed prefix runs as that mode and the split-button **check mark reflects the buffer's current mode** (the picker is a convenience, not the only way in). Menu = an **S29** `DropdownMenu` anchored to the caret. |

## R. Results, charting & workspace (v8 handoff)

The results panel + its status bar were substantially reworked in v6–v8. Per-tab
result state is **A5**; the UI is below.

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| R1 | Unified results status bar + workspace rework | ✅ Done | **Compiled + verified on Mac.** `results.rs` restructured into a grid/chart **toolbar** (Find — grid-only, live `N of M on page` count + clear · **Table/Chart** segmented toggle · **Refresh** icon = re-run · **Download** icon = export) + the state **body** + a single **unified 40px status bar** in every state: state dot (idle/run-pulse/err/plan/ok) + label + `· subtext` (`⌘↵ to run` / `scanning <target>` / error type / `N ms` / `N operators · physical|logical|measured`), the **snapshot clock chip** (`⏱ snapshot Xm ago`, `ran_at` on `WorkspaceRun`, `use_future` ticks 15s), and the **pager** (page-size ▲ dropdown + first/prev/`of M`/next/last) pinned right, grid-only. `view: ResultsView{Grid,Chart}` per result-set via `Action::SetResultsView`. **Chart pane = placeholder** until **R2**. Selection readout deferred to **R3**. Folds in **R7** (38px bar, gap 6px, 28px controls, refresh/download right). Old grid-only green-dot pager gone; app `statusbar.rs` (Events/History) untouched. **Original spec:** Rework the results panel to the v8 layout. A single **40px status bar** at its foot in **every** state (no-run · running · failed · grid · plan): state-coloured dot + monospace label + subtext (`⌘↵ to run` / `scanning <target>` / error type / `· N ms` / `N operators`), the **snapshot clock chip** (`⏱ snapshot 3m ago`, ticking ~15s), the live **selection readout** (R3), and the **pager** (page-size ▲ dropdown + first/prev/page-input `of M`/next/last) pinned right, shown **grid-only**. Plus the results **toolbar** row: **Find-in-results** (grid-only, live match count) · **Grid/Chart** segmented toggle (R2) · **Refresh** (re-run + reset snapshot label + clear selection) · **Download** (export). Today `workspace.rs` renders only a bare `pager()` and the app's `statusbar.rs` (Events + History) stays separate; the state-driven token, snapshot chip and selection readout don't exist. Snapshot timestamp is **per result-set** (pairs with A5). |
| R2 | Chart view (snapshot) | ⬜ Todo | New results view — full spec in **`CHART_SPEC.md`**. Grid/Chart toggle swaps the grid for a two-pane chart layout (left encoder strip + right canvas). Six types (bar / line / area / scatter / histogram / pie) on a **dependency-free canvas**, theme-aware via CSS vars. Encoders X / Y / Series / type + **Aggregate** (client-side GROUP BY over the snapshot, **default ON**) with `sum/avg/median/min/max/count/distinct`; scatter trendline; line/area moving-avg / running-total overlay. Schema-driven default encoding, always visible + overridable. **Guardrails** (never silently sample): >200k rows → too-big · aggregate-off >2k → raw-too-big · >60 groups → non-blocking hi-card banner; the **Add GROUP BY in SQL** CTA scaffolds real, user-owned SQL into a **new editable tab** (`date_bin`-bucketed for temporal X). Config **per result-set**; never re-queries source files. Real-engine upgrades (aggregate in DataFusion via `MemTable`, `approx_distinct`, window overlays) in §8. |
| R3 | Grid selection + live aggregate | ⬜ Todo | Spreadsheet-style selection on the results grid (§10): **cell/range** (click, drag-sweep, shift-extend; accent focus outline + tinted block), **row multi-select** (gutter click / drag / ⌘-toggle / shift-run), **column multi-select** (header click / drag / ⌘-toggle; persists across pages). **⌘A** / top-left `#` = whole page; **Esc** / empty-area click clears; cleared on Refresh + re-run. **⌘C** copies — a range → **TSV**, a row → **CSV w/ header**, a column → **CSV w/ header over the whole snapshot** (skipped when a text field is focused). **Live aggregate** in the status bar: `N cells · R×C` (or `K columns · N values`) + `sum / avg / min / max` over numeric cells (cell/row = current page, column = whole snapshot). |
| R4 | Copy affordances (cell / row / column / all) | ⬜ Todo | Right-click any result cell → copy menu (§10): **Copy cell**, **Copy row (JSON)**, **Copy row (CSV)**, **Copy column "&lt;name&gt;"** (all snapshot values), **Copy all as CSV** / **Copy all as Markdown**. Operates on the local snapshot honouring the active sort; CSV cells quote-escaped, nested values as inline JSON; confirms via a status-bar toast (`Copied 48,213 rows as CSV`). |
| R5 | Row detail (record view) | ⬜ Todo | Double-click a row's **number cell** (or "Open row detail" from its right-click menu) → the whole row as a vertical **key→value card** (field name + Arrow type left, value right); nested struct/list/map render as pretty JSON, null dimmed. Header **Row N of M** with prev/next stepping the whole snapshot (respects active sort), **Copy JSON / Copy CSV**, close + backdrop-close. Ideal for wide / nested tables. |
| R6 | Column sort (chevron, snapshot) | ⬜ Todo | Per-header **▲/▼ chevron** cycling **asc → desc → clear** (accent + name highlight when active). Sorts the **local result snapshot** (the same data pagination reads) — covers the whole result set, **nulls last**, re-paginates from page 1, **no re-query**. Remembered **per result-set**. Sort moved off the header **click** (which now selects the column for R3) onto the explicit chevron. |
| R7 | Results control-bar tighten | ✅ Done | **Folded into R1** (built, unverified). The results toolbar is now 38px, `gap:6px`, `padding:0 6px`, 28px controls, with the Table/Chart toggle + refresh + download cluster hugging the right edge. |

## C. Outstanding from v1

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| C1 | Project persistence + app-config store | ✅ Done | `.strata` save/load + `preferences` app-config (recents); source paths relative-when-inside-project; RustRover directory-based open; centralized autosave via `is_durable`. **Thin:** autosave not debounced; no scratch-SQL dirty indicator. |
| C2 | Real Export | ✅ Done | `COPY … TO` for CSV / JSON / Parquet / Arrow (+ partitioned Hive dir), bounded memory; redesigned modal (scope / format / options / partition chips / live preview); clipboard md/tsv/csv/json. See `EXPORT_OPTIONS.md`. |
| C3 | Table Config validation states | ✅ Done | Synchronous bounded fs scan: format-match, matched-count, directories-only partitioning gate, **real `key=value` partition detection** + per-key type pickers, identifier check, ≥1-path, disabled-until-valid, failed-registration cleanup; combined file/folder picker; single-file format auto-detect. Remaining → C10. |
| C4 | Open catalog SQL in its own tab | ✅ Done | `open_in_tab` — edit-view / SELECT * / open-saved-query reuse a same-named tab or append; never clobber the active tab. |
| C5 | Command palette depth | 🟡 Thin | Typed `PaletteCommand` + concrete actions done. Remaining: columns group, full keyboard nav (↑↓/Enter/hover), per-item metadata + shortcut hints, type-coloured icons. |
| C6 | Inspector stats | ⛔ Dropped | v5: inspector is **free-metadata only** (name · Arrow type · source badge · nested-fields tree). No fabricated distinct/mean/histogram. **Revived honestly in v10 → C14.** |
| C7 | Find-in-results → Filter-in-SQL | ⛔ Dropped | Not wanted. |
| C8 | Nested cells as real JSON | 🟡 Thin | Emit parseable JSON (arrow-json) in the cell popover + recursive nested schema in the inspector. |
| C9 | PART badges on auto-registered tables | ⬜ Todo | Echo partition metadata in the `Registered` event so partitioned tables show `PART`. |
| C10 | Homogeneous-folder / JSON-shape detection | ⬜ Todo | Schema-consistency report (`100 files · consistent ✓` / `97 match · 3 extra column`). |
| C11 | Theming controls | ⛔ Superseded | By S2 + S3 (token system + Settings). v5 drops the accent-swatch picker; density/zebra stay settings. |
| C12 | Undo/redo in the SQL editor (per tab) | ⬜ Todo | ⌘Z / ⇧⌘Z in the active tab's buffer. Open Q: rely on `dioxus-code` history vs an explicit per-workspace stack (survives tab switch/reload). Distinct from `ReopenTab`. |
| C13 | Import (read) options for CSV/JSON | 🚧 Blocked | CSV read options (delimiter/header/null/quote) → `TableSpec` → `register_external`; JSON = NDJSON-only. **Design pending** (`IMPORT_OPTIONS.md`); Alex getting an updated design. |
| C14 | Column / table profiling (revived, honestly) | ⬜ Todo | **v10 — supersedes the dropped C6.** Inspector splits into **FROM FILE** (footer-derived, never fabricated — Arrow type · format · row/null count + completeness · min/max where the footer has them) and **PROFILE** (**scan-derived**, computed only on explicit action, then **cached as catalog-level metadata** shared across tabs, stamped `scanned Nm ago`, refreshable). **`Profile table`** (catalog row-menu + empty-zone button) scans **every scalar column in one pass**: numbers → distinct/mean/median/p95 + histogram; strings/bool → distinct/top-values (+ empty-%); temporal → min/max span + buckets. **Nested (`Struct`/`List`/`Map`) never element-traversed** — a container **presence bar** (null/empty/filled) + "reach a leaf via SQL" note. **Cost-honest:** >50 files → cost-confirm first (distinct can't merge across files → full scan). **View as query** (⟨/⟩) drops the generated `SELECT count(distinct…), avg…, approx_percentile_cont…` into a new tab; ↻ re-scans. Cache in-memory only (lost on restart / re-registration / re-scan) so a number can't drift. |
| C15 | Catalog re-scan (re-infer ListingTable file sets) | ⬜ Todo | **v10.** A `ListingTable`'s file list is fixed at registration — new partitions/files in a glob stay invisible until re-inferred. Add a **↻ re-scan** button to the catalog header (between filter + collapse): re-lists sources (spinner), reports to the event log (`Re-scanned catalog · events +3 files (new partition month=02), orders +18 files`). Because the file set changes, a re-scan **invalidates all cached profiles** (C14) — log appends `· N cached profile(s) invalidated` and the PROFILE zone reverts to empty. |
| C16 | Configure/Register table tidy — honesty pass (v10) | ⬜ Todo | **v10 refinements to New/Configure-table** (non-cloud; cloud → S21): (1) error/validating **status renders below** the Hive section (above the footer); (2) **drop fabricated glob counts** — per-path chip shows the *shape* (`file`/`directory`/`glob`), summary reads `N path(s) ready · files resolved at query time`; (3) **remove the invented pipeline stepper** (Resolve→Footers→Unify→Register) → one honest `Registering table…` spinner + plain `Couldn't register table` panel; (4) **basic validation gates the button** (empty name / zero paths disables Register); (5) **empty summary shows nothing** (no `✓ 0 paths` false success); (6) SOURCE PATHS hint → `.ps-tip` tooltip with a worked example per mode + supported stores + the one-store rule. Extends C3. |
| C17 | Export dialog + options — v11/v12 alignment | ⬜ Todo | Reconcile the Real Export modal (**C2** / `EXPORT_OPTIONS.md`) with v11 + FEATURES §13: (1) section-group labels → **UPPERCASE monospace** (house convention; segment *item* labels like Comma/Tab/Zstd stay sentence-case), FORMAT header letter-spacing `.8px`; **(v12)** CSV **delimiter** is now a **free text input** (S28 `TextInput`; default `,`, resolves escape sequences — `\t`→tab, `\n`→newline — in the CSV command builder), retiring the segmented Comma/Tab/Semicolon/Pipe control; CSV/Parquet **compression** is now a **custom app-themed dropdown** (**S29** `Select`), not a segmented control; (2) per-format write options match the real surface — **CSV** header / delimiter / null-as + advanced (quote / escape / double-quote / compression); **JSON = NDJSON + compression only** (no array/columnar/pretty); **Parquet** compression + level + statistics + row-group + writer-version + dictionary; **Arrow IPC = no write options**; (3) **Hive-style partitioned export** (click columns in order → directory tree, size labelled `· directory`, confirm becomes **Choose folder…**). Some (partitioned export, basic CSV/Parquet opts) already exist from C2 — audit each. |

---

## Suggested order (remaining)

The **v10 handoff** (section **R** + S17–S24 + B11a/b) is the new headline. Rough
order: **A5** per-tab result state is the enabler → **R1** results status-bar /
workspace rework → **R6/R3** column sort + selection → **R4/R5** copy + record
view → **R2** chart view → **S17** engine settings → **S20** plan v3. Older
section **S**, in rough dependency order: **S8** tab-bar controls → **S19** tab
drag-reorder → **S7** autocomplete → **S11** File menu → **S14** query
cancellation (adds the Cancel button to S13's Running state).
Theming follow-ups: **S16** author-friendly theme files, OS-sync live listener,
live theme reload, plugin theme dirs.

Remaining pre-v5: **C5** palette depth · **C8** nested JSON · **C9** PART badges ·
**C10** folder/JSON-shape detection · **C12** editor undo/redo.

Blocked on design: **B10** open-in current-vs-new window · **C13** import options.

Architecture cleanups: **A3** + **A4** both shipped — overlays run on reusable
containers + a per-window store; modal form state (Config/Export) is off
`AppState`. See `OVERLAY_ARCHITECTURE.md`. **A5** (per-tab query state + view/
saved-query dirty tracking) is the remaining foundation item.

Small follow-ups: debounce autosave · stream-drain ANALYZE to bound memory · B8
window geometry in logical (not physical) px · typed source paths don't
auto-relativize (only picks do) · verify the unified floating-window chrome
(A3/A4 `Window`) matches the v8 spec — non-blocking / no backdrop, multiple open
at once, click-to-raise z-order, per-session geometry. Header-as-native-title-bar
(macOS `hiddenInset`) is **implemented** — only the v10 visual alignment (88px
traffic-light inset · project-switcher dropdown offset) remains. (Scratch-SQL dirty
indicator → A6; plan self-time → S20.)

## Done (reference)

Shell / 3-pane layout · DataFusion 43 engine on a background thread + channels ·
DDL policy gate (sqlparser AST) · snapshot-streaming pagination (single pass,
bounded memory) + cleanup lifecycle · `tracing` of all engine failures ·
`dioxus-code` editor (sql/json) · macOS integrated title bar · global scrollbar
theming. Plus everything marked ✅ above (A1–A4, B1–B9, S1–S6/S9/S10/S13/S15,
C1–C4). (S12 is now 🟡 Thin — v3 rework tracked as S20; A5 is Todo.) The old runtime `sample.rs` generator is gone — dev launch opens the
bundled `sample/sample.strata` via the real project-open path.
