# Strata

Read [AGENTS.md](AGENTS.md) — the project conventions, architecture summary, testing rules and
checks, written for humans and AI agents alike.

Build and run with `cargo run` at the repo root (the first build compiles Skia and DataFusion —
give it time). The docs index is [docs/README.md](docs/README.md); start with
[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
